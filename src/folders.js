import * as store from './store.js';import {el,openModal,closeModal,modalLayout,toast} from './ui.js';
export async function createFolder(name){const trimmed=String(name||'').trim().slice(0,40);if(!trimmed)throw new Error('Enter a folder name.');const folders=await store.all('folders');if(folders.some(f=>f.name.toLocaleLowerCase()===trimmed.toLocaleLowerCase()))throw new Error('That folder already exists.');const row={id:store.makeId('f'),name:trimmed,order:folders.length,createdAt:Date.now()};await store.put('folders',row);return row}
export async function deleteFolder(id){const items=await store.all('items');for(const item of items.filter(i=>i.folderId===id))await store.put('items',{...item,folderId:null,updatedAt:Date.now()});await store.remove('folders',id)}
export function folderPicker(folders,current,onPick,{manage}={}){const list=el('div',{class:'folder-list'});const rows=[{id:'all',name:'All folders'},{id:'unsorted',name:'Unsorted'},...folders.sort((a,b)=>a.order-b.order)];rows.forEach(f=>list.append(el('button',{type:'button',class:`folder-option${current===f.id?' active':''}`,onclick:()=>{closeModal();onPick(f.id)}},[el('span',{text:f.name})])));if(manage)list.append(el('button',{type:'button',class:'folder-option',onclick:()=>{closeModal();manage()}},[el('strong',{text:'Manage folders'})]));openModal(modalLayout('Folders',list,[el('button',{class:'soft-btn',type:'button',text:'Close',onclick:closeModal})]))}
export async function manageFolders(onChange){const paint=async()=>{const folders=(await store.all('folders')).sort((a,b)=>a.order-b.order),items=await store.all('items');const list=el('div',{class:'folder-list'});list.append(el('button',{class:'folder-option',type:'button',onclick:async()=>{const name=prompt('Folder name');if(name)try{await createFolder(name);toast('Folder created.');paint()}catch(e){toast(e.message)}}},[el('strong',{text:'+ New folder'})]));folders.forEach(f=>{const count=items.filter(i=>i.folderId===f.id).length;list.append(el('div',{class:'row'},[el('span',{class:'row-label',text:f.name}),el('span',{class:'row-value',text:`${count} item${count===1?'':'s'}`}),el('button',{class:'mini-btn danger',type:'button',text:'Delete',onclick:async()=>{if(confirm(`${count} items will move to Unsorted. Items are not deleted.`)){await deleteFolder(f.id);toast('Folder deleted.');onChange?.();paint()}}})]))});openModal(modalLayout('Manage folders',list,[el('button',{class:'soft-btn',type:'button',text:'Close',onclick:closeModal})]))};paint()}
