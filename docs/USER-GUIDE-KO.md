# 사용 안내

1. 첫 화면의 `+ Add link`에서 URL과 제목을 저장합니다.
2. `Inbox`, `Reading`, `Done` 탭으로 상태를 나눕니다. 원문이나 저장본을 열면 Inbox 항목은 Reading으로 이동하며 Done은 직접 지정합니다.
3. 카드의 `…`에서 폴더 이동, 핀, 링크 복사, 삭제를 사용합니다. 삭제 후 5초 동안 `Undo`할 수 있습니다.
4. Safari에서 저장한 HTML은 `Settings → Import HTML`로 가져옵니다. `Read`에서 본문을 선택한 뒤 다섯 색 중 하나를 누르면 하이라이트가 저장됩니다.
5. 항목 화면의 `Export`로 Obsidian용 Markdown을 받습니다.

## Daybook 독서시간 (2026-09-01)

- Cove 안의 **Reader**로 읽는 시간은 정확하게(`historyAccuracy: exact`) 측정되어 Daybook에 `8:10–8:36 PM · (26m)`처럼 표시됩니다.
- **`Open in Safari`**로 외부에서 읽은 시간은 종료 시각을 알 수 없어 근사값입니다. Cove가 다시 화면에 나타난 시점까지의 경과 시간으로 계산하며, Daybook에는 `8:10–8:36 PM · (~26m)`처럼 물결표(`~`)가 붙어 정확한 값과 구분됩니다.
  - 30초 미만 만에 돌아오면(잘못 눌렀다가 돌아온 경우) 기록하지 않습니다.
  - 60분이 넘어서 돌아오면 시각만 남기고(`8:10 PM · 제목`) duration은 만들지 않습니다.
  - 이 측정 때문에 링크 열기·저장·상태 변경 같은 원래 동작이 지연되거나 막히지 않습니다.

새 백업 파일(schema 3)은 2026-09-01 이후 버전의 Cove에서만 복원됩니다. 자세한 내용은 `BACKUP-RESTORE-KO.md`를 참고하세요.
